export type ProductType = {
  name: string;
  price: string;
  url: string;
};

export type ProductDetailsType = {
  title: string;
  price: string;
  description: string;
  sizes: string[];
  imageURL: string;
};
